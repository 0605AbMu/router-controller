name: Build & Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: router
            asset_name: router-linux-x86_64
            pyinstaller_args: ""

          - os: windows-latest
            artifact_name: router.exe
            asset_name: router-windows-x86_64.exe
            pyinstaller_args: ""

          - os: macos-latest
            artifact_name: router
            asset_name: router-macos-universal
            pyinstaller_args: "--target-arch universal2"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Python o'rnatish
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Dependencylarni o'rnatish
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install git+https://github.com/AlexandrErohin/TP-Link-Archer-C6U
          pip install typer[all] rich keyring

      - name: PyInstaller bilan build qilish
        run: |
          pyinstaller --onefile --name router ${{ matrix.pyinstaller_args }} src/router_controller/main.py

      - name: Artifact yuklash
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: dist/${{ matrix.artifact_name }}

  release:
    name: GitHub Release yaratish
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Barcha artifactlarni yuklab olish
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Fayllarni release uchun tayyorlash
        run: |
          mkdir -p release
          cp artifacts/router-linux-x86_64/router           release/router-linux-x86_64
          cp artifacts/router-windows-x86_64.exe/router.exe release/router-windows-x86_64.exe
          cp artifacts/router-macos-universal/router         release/router-macos-universal

      - name: Release yaratish va fayllarni yuklash
        uses: softprops/action-gh-release@v2
        with:
          name: "router-controller ${{ github.ref_name }}"
          body: |
            ## router-controller ${{ github.ref_name }}

            TP-Link router boshqarish uchun standalone CLI.
            Python yoki pip **kerak emas** — faqat yuklab oling va ishga tushiring.

            ### O'rnatish

            **macOS (Apple Silicon + Intel — Universal)**
            ```bash
            curl -L https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/router-macos-universal -o router
            chmod +x router && sudo mv router /usr/local/bin/
            ```

            **Linux**
            ```bash
            curl -L https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/router-linux-x86_64 -o router
            chmod +x router && sudo mv router /usr/local/bin/
            ```

            **Windows**
            `router-windows-x86_64.exe` faylini yuklab oling, istalgan joyga qo'ying va PATH ga qo'shing.

            ### Birinchi sozlash
            ```bash
            router config set --host http://192.168.0.1 --password PAROLINGIZ
            ```

            ### Buyruqlar
            ```
            router status
            router devices
            router wifi status
            router wifi channel 2.4 6
            router reboot
            ```
          files: |
            release/router-linux-x86_64
            release/router-windows-x86_64.exe
            release/router-macos-universal
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
